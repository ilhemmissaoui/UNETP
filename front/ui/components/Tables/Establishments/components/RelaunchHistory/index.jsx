import React from 'react';

import List from './List';

const RelaunchHistory = ({ data }) => {
    return (
        <>
            <List data={data} />
        </>
    );
};

export default RelaunchHistory;
